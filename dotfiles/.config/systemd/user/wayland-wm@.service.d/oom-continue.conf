[Service]
# 2026-09-04: StarSavior.exe 内存耗尽触发内核全局 OOM 时，systemd 默认
# OOMPolicy=stop 会把整个图形会话单元判为 oom-kill 失败并触发
# OnFailure=wayland-session-shutdown.target 注销桌面。
# 设为 continue：单元内任意进程被 OOM 杀只记日志，会话继续运行。
# 依据 systemd.service(5) OOMPolicy=；对 Hyprland/niri 等 UWSM 会话统一生效。
OOMPolicy=continue
