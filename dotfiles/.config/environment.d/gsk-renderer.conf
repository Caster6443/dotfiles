# Force GTK4 apps (Nautilus etc.) to use the OpenGL renderer instead of Vulkan.
# On hybrid-GPU laptops the Vulkan backend can wake up the discrete GPU,
# which makes apps like Nautilus slow to start.
GSK_RENDERER=opengl
