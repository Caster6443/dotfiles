return {
  {
    "saghen/blink.cmp",
    opts = {
      -- Tab 接受补全（VSCode 风格）
      -- 可选值：default = C-y 接受 / super-tab = Tab 接受 / enter = 回车接受 / none = 不绑
      keymap = { preset = "super-tab" },
    },
  },
}
