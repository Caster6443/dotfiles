// 添加如下行以在启动时运行进程。
// 请注意，以会话模式运行 niri 支持 xdg-desktop-autostart（标准桌面自启），
// 这可能使用起来更方便。
// 请参阅下方的按键绑定 (binds) 部分以获取更多启动示例。

// 要运行 shell 命令（包含变量、管道符等），请使用 spawn-sh-at-startup:
// spawn-sh-at-startup "qs -c ~/source/qs/MyAwesomeShell"

//监听文本复制
spawn-at-startup "wl-paste" "--type" "text" "--watch" "cliphist" "store"
//监听图片复制
spawn-at-startup "wl-paste" "--type" "image" "--watch" "cliphist" "store"
// 强制同步环境变量到 DBus/Systemd 会话
// 这能修复 Nautilus、Loupe 等通过 DBus 启动的应用没有中文、不吃主题的问题
spawn-at-startup "dbus-update-activation-environment" "--systemd" "LANG" "LC_MESSAGES" "GSK_RENDERER" "XMODIFIERS" "GTK_IM_MODULE" "QT_IM_MODULE" "QT_QPA_PLATFORMTHEME"
//启动noctalia v5 shell（C++ 原生，替代 v4 Quickshell 版）
spawn-at-startup "noctalia"
//解决蓝牙/USB耳机连接时，输出设备不自动切换的问题
spawn-at-startup "pactl" "load-module" "module-switch-on-connect"

//蓝牙/USB耳机的媒体键（AVRCP）转发给 MPRIS —— 对应 Hyprland 侧 execs.lua 的 mpris-proxy
spawn-at-startup "mpris-proxy"
//剪贴板「键入最后一项」快捷键（Mod+Ctrl+Shift+V）依赖 ydotool 的守护进程
spawn-at-startup "ydotoold"
//清理 30 天前的回收站 —— 对应 Hyprland 侧 execs.lua 的 trash-empty 30
spawn-at-startup "trash-empty" "30"

//休眠监听功能
//2026-09-14 修正：本机并没有装 hypridle（`pacman -Q hypridle` 无此包，/usr/bin 下也没有），
//这一行一直在静默失败，等于 niri 会话没有自动锁屏/息屏/挂起。
//要恢复空闲行为，二选一（决定后再取消下面注释或换写法）：
//  1) 安装 hypridle 并写 ~/.config/hypr/hypridle.conf；
//  2) 改用 noctalia 自带的 idle 行为与锁屏（noctalia 设置里有 Idle 段）。
//spawn-at-startup "hypridle"
spawn-at-startup "/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1"
spawn-at-startup "fcitx5" "-d"

hotkey-overlay {
    // 取消注释此行以禁用启动时的“重要快捷键”弹窗。
        skip-at-startup
}
