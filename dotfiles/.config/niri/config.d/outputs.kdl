// 显示器（输出）配置。会话内用 `niri msg outputs` 查看实际连接与名称；
// 完整选项说明见官方模板 /usr/share/doc/niri/default-config.kdl。
//
// 2026-09-14 清理：删除了原先注释掉的旧 eDP-1 节点（mode 1920x1080@120.030 / scale 2 /
// position 1280x0）。那是换屏或调分辨率之前的残留，niri 只读未注释的节点，留着只会在
// 排查显示器问题时误导人。
//
// 注：走独显（RTX 4060）的接口是 card2 上的 DP-9 与 HDMI-A-1；由于 config.kdl 的 debug 段
// 让 niri 完全忽略独显的 DRM 节点，niri 会话里外接屏请用核显侧接口（card1 的 DP-1..8）。

output "eDP-2" {
    mode "2560x1440@165.003"
    scale 1.5
    position x=1066 y=0
    focus-at-startup
}

output "DP-3" {
    mode "2560x1600@119.998"
    scale 1.5
    position x=0 y=0
    transform "90"
}
